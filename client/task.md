# Task and the corresponding problem it solves.

## /code page :
we need a /code page, where the interface is unlike GPT and modern, a sidebar of all the previous chats from the user, and each chat is only limited to one paper, all files will stay for that alone. and the text area is used to take user's feedback at any point in time they can terminate that request and start a new one with a new query, that chat should be there, dont disable the text-area and let the user type still.

the sidebar is on the left, and the righ side the majority side, there is the stuff from teh sandbox, all the files, traces and letting the user's pick the files with syntax hilighting for python and switch btw files and read them. the bottom will have a chatbox where theypass on queries to the model's code, and this is only for pro and orgs, if its free they dont get anything apart from the file system. 

all of this should be compliant with the shit from backend, all traces should be retrieved from upstash, ill add the envs for it, just use upstash in nextjs feel free to npm install @upstash/redis for using it in nextjs

the layout headers should change and have the user's login creds, if they click on the bubble on the user's icon, they can logout -> if they do then we move back to the landing page with a thank you.

## CTA : in teh CTA the Get Started should go to a new page

The cta buttons should have Get Started -link-> /code 
and nothing else. remove the google signin from teh CTA and leave it on the header
add a How it works button on the CTA that takes the users to the agent's slides.

## Footer :
cutdown on the space in teh bottom, and add the links for them like this https://x.com/d_leap07 for x and linkedin nobody wants, and github should take them to the github for polarisai-implementations page :).
cut down on the space in teh bottom again. 


## section wise changes: 
instead of it being a raw flat line that differentiates btw two divs, make the second screen with rounded-top, so it creates a goo illusion and add mask after the end of the HERO section. it will look aesthetic, the tailwind masks can be added anywhere, use radial gradients, stronger blue and yellow and green culminations in gradients are ideal make sure they're aesthetic and easy on the eye. 

